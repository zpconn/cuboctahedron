# Scripts

This directory is reserved for external helper scripts.

Scripts may generate exact rational certificates or independently check
generated data, but they are not trusted proof. Lean must check every generated
certificate used by the final theorem.
